[[GitHub]]

*Note: this can be done after commit and before push in git bash here*

1. git commit 

2. git log --> copy the reference of the commit (which you want to revert) -->git revert "reference no."
3.  if you want to add revert message: Esp + I (it activates insert option)
4. Particular file removal:
     git rm "file path .file extension "